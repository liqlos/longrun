# Modifying the harness or contract mid-run
- Observable outcome, criteria, constraints, non-goals change only via `longrun rebase` (new contract version, reason recorded, all criteria reset to FAIL).
- Budgets never grow mid-run. Baseline never moves. New run = new UUID.
