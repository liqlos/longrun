# Adapters
An adapter subclasses longrun.adapters.Adapter: default evidence kinds per criterion kind, baseline commands, allowed builder commands, short builder/evaluator guidance, optional helpers (e.g. vr_visual capture manifest). Adapters never read or write run state.
