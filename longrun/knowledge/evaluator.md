# Evaluator rules (detail)
- Read-only. Open cited artifacts. Re-run cheap read-only checks. Ignore builder prose.
- One verdict per criterion; PASS needs criterion-bound, current-revision evidence of a required kind.
- failure_signature must be stable across evaluations for the same underlying problem.
